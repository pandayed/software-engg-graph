Handles HTTP requests and serves static files (HTML, CSS, images).

Can also forward requests to application servers.

**Examples:** Nginx, Apache, Caddy.

**Use case:** Serving a company's public website pages.

